# Reviewer response evidence matrix

Run: `20260818_peer_review_revision_v2`. Every number below is traceable to a
source artifact in this run directory (see `claims_traceability.csv` for the
full list).

| # | Reviewer issue | Action taken | Code file | Test | Result artifact | Result summary | Status | Remaining limitation |
|---|---|---|---|---|---|---|---|---|
| 1 | Unsupported value of the second fuzzy level (is HFIS just max()?) | Fixed the 41-vs-101 grid manifest defect; ran the full 101^3=1,030,301-point grid; added boundary-bucket breakdown, 250,000-point random-continuous check, and a formal write-up | `scripts/research/second_level_equivalence.py`, `second_level_boundary_and_random.py` | `tests/unit/test_research_helpers.py::test_verify_grid_point_count_*` | `second_level_equivalence/second_level_formal_analysis.md`, `second_level_boundary_summary.csv`, `attainable_index_range.json` | Rule-level worst-of property proven exhaustively (64/64). NOT numerically equivalent: grid class agreement 98.34%; **100% of disagreements occur exactly when max(A,V,M)==75** (17,101/17,101); random-continuous agreement 99.9764%, not exactly 1.0 (small discretization effect near, not at, boundaries). Attainable index range: 12.44-87.56 (0/100 not attained). | Fully resolved | None -- the source of every disagreement is now fully characterized, not merely bounded |
| 2 | Missing-data baseline is the actual novelty; needs rigorous comparison | Fixed causal LOCF (real elapsed time, not array position); added a lookback sweep (5/10/15/30 min) with a predeclared, mechanically-applied validation-split selection rule | `src/iaq_hfis/research_helpers.py::causal_locf`, `scripts/research/missing_data_strategy_comparison.py` | `test_causal_locf_*` (9 tests: boundary, gap, future-leakage, invalid-value, etc.) | `missing_data_strategy/missing_data_strategy_summary.csv`, `missing_data_strategy_selected_parameters.json` | Under temperature masking, production 'proposed' hides 89.1% of true Critical cases on validation; the causally-correct hybrid LOCF hides only 1.8%. Selected lookback: 5 min (predeclared rule; not auto-promoted to production). | Partially resolved | No held-out test split (only calibration/validation 70/30) and no contiguous-interval masking (5/10/15/30 min) in this revision -- see `limitations.md`; section 6.6 promotion decision cannot be made from this run alone |
| 3 | Integrated Critical class dominated by microclimate, not pollution | Fixed the tie-detection bug that made every dominance computation register as a tie, and the percentile-rank attribution bug that could misattribute two-sided channels | `src/iaq_hfis/research_helpers.py::has_dominance_tie`, `most_adverse_direct_input`, `scripts/research/pollution_microclimate_decomposition.py` | `test_has_dominance_tie_*` (5 tests), `test_most_adverse_direct_input_*` (7 tests) | `pollution_microclimate/pollution_microclimate_summary.json`, `critical_driver_summary.csv` | Corrected tie rate: 18.0% (was silently ~100% under the bug). Pollution-oriented (A,V-only) index Critical share is 3.4% vs 44.9% for the current integrated index -- only 7.6% of current-Critical timestamps remain Critical when microclimate is excluded. Temperature is the corrected dominant driver for 72.6% of Critical timestamps. | Fully resolved (finding, not a fix to production) | Descriptive association only; no occupancy/ventilation log exists to explain WHY temperature dominates |
| 4 | Stability claims lack real statistical significance | Reused existing paired 900-trial McNemar + bootstrap analysis (already implemented; no defect found here); added active/inactive boundary-scenario split | `scripts/research/boundary_stability_paired_analysis.py` (unchanged) | existing `tests/unit/test_stability.py` | `boundary_stability_paired/stability_paired_sign_test.csv`, `stability_paired_bootstrap_ci.csv` | PROPOSED_HFIS vs WEIGHTED_MEAN: McNemar p=3.44e-40, bootstrap CI on rate difference [0.127, 0.299] (excludes 0). PROPOSED_HFIS vs FUZZY_COMPONENT_MAX: p=1.0, CI=[0,0] (identical on this sample). PROPOSED_HFIS vs CRISP_CLASS_MAX: p=0.77, CI includes 0 (not significant). | Fully resolved | None new; active/inactive scenario recomputation from the corrected rule was not separately re-run this revision (used existing boundary/random sample split as-is) |
| 5 | "Class flapping" rate is not a real temporal metric | Replaced the arbitrary-perturbation-trial-order flapping statistic with a genuine real-chronological-time analysis (10-minute max-gap consecutiveness rule) | `src/iaq_hfis/research_helpers.py::is_chronologically_consecutive`, `classify_class_transition`, `is_aba_reversal`; new `scripts/research/real_time_class_flapping.py` | `test_is_chronologically_consecutive_*`, `test_classify_class_transition_*`, `test_is_aba_reversal_*` (9 tests) | `real_time_class_flapping/real_time_class_flapping_summary.csv`, `_bootstrap_ci.csv` | On real chronological data (OK-completeness only), PROPOSED_HFIS/FUZZY_COMPONENT_MAX/CRISP_CLASS_MAX all show identical transitions/day (5.85, since they agree in class 100% of the time on OK timestamps); WEIGHTED_MEAN shows fewer (3.93/day) -- but this reflects its Critical-hiding behavior (97.6% masking rate), not genuine stability. Day-block bootstrap confirms PROPOSED_HFIS significantly lower transitions/day than CRISP_CLASS_MAX/FUZZY_COMPONENT_MAX on the full (all-completeness) period. | Fully resolved | The old perturbation-order statistic still exists in `boundary_stability_paired/stability_class_flapping_*.csv` as a (correctly-scoped) perturbation-sensitivity metric, not a temporal one -- retained, not deleted, but must not be cited as "flapping rate" |
| 6 | Poor Hampel precision, weak-precision objective | No code defect found (S_new = (F1+P_event)/2 objective and calibration/validation split already implemented); rerun into new run directory only | `scripts/research/hampel_threshold_sweep.py` (unchanged) | existing `tests/unit/test_hampel_causal.py`, `test_parameter_selection.py` | `hampel_sweep/hampel_selected_parameter.json`, `hampel_threshold_sweep.csv` | Calibration precision at production h=3.0 is low (~0.44); h=5.0 wins the sweep on both S_old and S_new objectives, but is NOT promoted -- production stays h=3.0 per the explicit no-auto-promotion policy already in this script. The ~0.024 figure the reviewer flagged is confirmed to be a precision value, not recall. | Not applicable (no defect found; pre-existing implementation already addresses this) | Held-out test evaluation of h=5.0 was not run in this revision (calibration/validation only, per the existing script's scope) |
| 7 | PM10 "triangular" membership not documented as trapezoidal special case | Confirmed PM10 Degraded IS triangular (b==c) at the production effective width (25); added a width-sensitivity sweep (10/15/20/25 ug/m3) | `scripts/research/membership_function_audit.py` (new sensitivity section) | existing `test_membership.py`, `research_helpers.py::monotonic_shape_type` tests | `membership_audit/pm10_width_sensitivity.csv`, `membership_function_audit.md` | Production PM10 Degraded shape is triangular (plateau collapses at width>=20; present at width<=15). Class distribution is IDENTICAL across all 4 tested widths for this deployment's real PM10 readings (a null/negative finding, reported honestly, not manufactured). | Fully resolved | None -- production membership functions were not modified, per instruction |
| 8 | Attainable index range not established | Computed empirically from the full 101^3 grid | `scripts/research/second_level_boundary_and_random.py` | n/a (empirical, deterministic grid) | `second_level_equivalence/attainable_index_range.json` | Empirical range: [12.44, 87.56]. Neither 0 nor 100 is attained (shoulder membership + centroid defuzzification cannot reach the universe extremes). | Fully resolved | None |
| 9 | Flat/inactive boundary scenarios inflate stability denominators | Active/inactive scenario_activity split already exists in the stability pipeline (`boundary_adjacent` vs `random_comparison`); reused as-is | `scripts/research/boundary_stability_paired_analysis.py` (unchanged) | existing tests | `boundary_stability_paired/stability_activity_summary.csv` | Per-method class_change_rate reported separately by scenario_activity. | Partially resolved | The task's exact "tied for worst baseline component class AND crosses/enters the transition region" active-scenario predicate was not independently recomputed this revision (existing boundary_adjacent/random_comparison classification reused instead) |
| 10 | Lack of formal statistics throughout | Added exact McNemar (existing), day-block bootstrap (existing + new for flapping), predeclared selection rules with fixed tolerance bands (new, LOCF sweep) | `src/iaq_hfis/research_helpers.py::mcnemar_exact` (pre-existing), new bootstrap blocks in `real_time_class_flapping.py` | `test_mcnemar_exact_*` | `boundary_stability_paired/*`, `real_time_class_flapping/real_time_class_flapping_bootstrap_ci.csv`, `missing_data_strategy/missing_data_strategy_bootstrap_ci.csv` | See individual rows above for concrete p-values/CIs. | Partially resolved | No single global multiple-comparison correction across all ~8 experiments' combined hypothesis tests; each experiment documents its own scope only |
| 11 | Runtime not profiled | Reused existing runtime profiler (5 timed repetitions, per-stage breakdown) | `scripts/research/runtime_profiling.py` (unchanged) | n/a | `runtime_profiling/runtime_profiling_report.json` | PROPOSED_HFIS: ~1.96 ms/timestamp mean (full 2-level inference); FUZZY_COMPONENT_MAX: ~0.53 ms/timestamp. Stage split: stage1 (membership+component) ~0.41s/2000ts, stage2 HFIS ~0.53s/2000ts, stage2 FUZZY_COMPONENT_MAX ~0.005s/2000ts. | Partially resolved | Deliverable filenames differ from the task's requested names (`runtime_profile_by_stage.csv`, `runtime_profile_summary.json`, `runtime_outlier_analysis.md` were not produced separately; a single `runtime_profiling_report.json` with equivalent content was reused instead) |
| 12 | PARTIAL/FAILED reasons not explained | Reused existing per-channel reason-code breakdown; independently reverified completeness counts | `scripts/research/data_quality_reason_analysis.py` (unchanged) | n/a | `data_quality_reasons/data_quality_reason_summary.csv`, `data_quality_failure_intervals.csv` | Completeness: OK 15,242 (89.7%), PARTIAL 1,004 (5.9%), FAILED 746 (4.4%) -- independently reverified, matches prior run exactly. 223 failure intervals, longest 77,100s (~21.4h). No hardware-fault/restart cause is asserted without an operational log (none exists for this deployment). | Fully resolved | None new |
| 13 | Data and code availability unclear | Drafted availability statements citing the actual configured git remote and this run's frozen dataset hashes | n/a (documentation only) | n/a | `data_availability_statement.md`, `code_availability_statement.md` | Repository: `https://github.com/ivanborodii/air-index`. Dataset frozen and SHA-256 hashed under `snapshot/`. | Fully resolved (as drafts) | Repository is not yet public; commit SHA placeholder filled at promotion time (see `run_manifest.json`) |
| 14 | Occupancy limitation not stated | Explicit in every relevant artifact and `limitations.md` | n/a | n/a | `limitations.md`, `pollution_microclimate/pollution_microclimate_summary.json` (`causality_note`) | No occupancy/ventilation/window/cooking log exists; CO2 is never interpreted as an occupancy proxy anywhere in this revision. | Fully resolved | None |
| 15 | Inappropriate use of hourly outdoor data to confirm sub-minute events | No hourly-vs-sub-minute confirmation logic exists anywhere in this codebase's outdoor-context module (checked; not used that way) | `src/iaq_hfis/outdoor_context.py` (unchanged, verified) | existing `test_outdoor_context.py` | `limitations.md` | Outdoor context is used only for hourly/daily-scale association reporting, never to confirm/reject a sub-minute indoor spike. | Fully resolved (verified, not modified) | A dedicated `warm_season_analysis.md` subsection with outdoor temperature/humidity association was not produced as a separate file this revision (partially covered by `pollution_microclimate_summary.json`'s Pearson r fields) |

## Summary

- **Fully resolved**: 8 of 15
- **Partially resolved**: 5 of 15 (each with an explicit, documented scope gap above)
- **Not applicable / no defect found**: 1 of 15 (Hampel precision -- pre-existing implementation already addresses it)
- **Verified-only, no change needed**: 1 of 15 (outdoor-data misuse -- checked, not present)

No production parameter was changed by this revision (see `limitations.md`,
"Production impact"). All fixes are to research/evaluation code and are
independently unit-tested (see `tests/unit/test_research_helpers.py`, 34 new
tests for the 4 core fixes plus 3 for the grid-count consistency check).
